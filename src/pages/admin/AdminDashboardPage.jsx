import {useEffect,useState} from 'react'
import {listAdminRows} from '../../lib/adminApi'
export default function AdminDashboardPage(){const [counts,setCounts]=useState({blogs:0,photos:0,videos:0}),[error,setError]=useState('');useEffect(()=>{Promise.all(['blogs','photos','videos'].map(listAdminRows)).then(([blogs,photos,videos])=>setCounts({blogs:blogs.length,photos:photos.length,videos:videos.length})).catch(err=>setError(err.message))},[]);return <section className="admin-page"><div className="admin-page-heading"><div><span>ADMINISTRASI</span><h1>Dashboard</h1></div></div>{error&&<div className="alert alert-danger">{error}</div>}<div className="admin-stat-grid"><Stat label="Artikel" value={counts.blogs}/><Stat label="Foto" value={counts.photos}/><Stat label="Video" value={counts.videos}/></div><div className="admin-info-card"><h2>Backend Supabase aktif</h2><p>Konten publik dibaca langsung dari PostgreSQL melalui Row Level Security. Login admin memakai Supabase Auth, sedangkan gambar artikel dan galeri disimpan di Supabase Storage.</p></div></section>}
function Stat({label,value}){return <div className="admin-stat"><span>{label}</span><strong>{value}</strong></div>}
