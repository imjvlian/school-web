import {useEffect,useState} from 'react'
import ArticleCard from '../components/ArticleCard'
import {getPublishedBlogs} from '../lib/contentApi'
import {isSupabaseConfigured} from '../lib/supabase'
export default function NewsPage(){const [articles,setArticles]=useState([]),[loading,setLoading]=useState(isSupabaseConfigured),[error,setError]=useState('');useEffect(()=>{if(!isSupabaseConfigured)return;getPublishedBlogs().then(setArticles).catch(err=>setError(err.message)).finally(()=>setLoading(false))},[]);return <main><section id="berita" className="page-offset py-5"><div className="container"><div className="header-berita text-center"><h2 className="fw-bold">Berita Kegiatan PonPes</h2></div><div className="row py-5 g-4">{loading&&<div className="col-12"><div className="alert alert-light border text-center py-5">Memuat berita...</div></div>}{error&&<div className="col-12"><div className="alert alert-danger">{error}</div></div>}{!loading&&!error&&articles.map(item=><div className="col-lg-4" key={item.slug}><ArticleCard article={item}/></div>)}{!loading&&!error&&!articles.length&&<div className="col-12"><div className="alert alert-light border text-center py-5">Belum ada artikel yang dipublikasikan.</div></div>}</div></div></section></main>}
